(() => {

    const init = () => {

        if (location.hostname !== "www.e-bebek.com" || location.pathname !== "/") {
            console.log("wrong page");
            return;
        }

        buildHTML();
        buildCSS();
        setEvents();
    }

    const buildHTML = () => {
       
    };



    const buildCSS = () => {

    }

    const setEvents = () => {

    }

    init();


})();
